Light is an iOS app that changes the screen from black to white, and back again, whenever the user taps the button. To successfully build it we used Xcode, set breakpoints and created outlets and actions.

